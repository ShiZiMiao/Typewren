# 更新日志

## [0.7.1] - 2026-09-22

发版前审查驱动的修复批次（66 项）+ 缩放体系重构。修复记录详见 AGENTS.md 决策 #20/#21。

### 新增 / 调整

- **内容区缩放**：Ctrl+= / Ctrl++（含小键盘+）放大、Ctrl+- 缩小、Ctrl+滚轮缩放（触控板捏合可用）；
  只缩放编辑/源码区域（50%–300%，步进 10%），标题栏/菜单栏/侧栏/状态栏不再跟着缩放
- **重置缩放 Ctrl+Shift+D**（D=Default；Ctrl+Shift+0 保留为不显示的备用判定，
  原 Ctrl+Shift+0 在多数中文输入法下被热键占用）
- 侧栏右下角 ‹/› 收起按钮移除（收起入口：状态栏「☰ 侧栏」/ Ctrl+\ / 视图菜单）

### 修复 — 数据安全

- 双击打开文件（带命令行参数启动）会**永久吞掉全部崩溃恢复草稿与会话**（数据丢失级）
- 保存间隙继续输入可能静默丢字；源码模式保存后退出可能误报"未保存"（保存基线不成对）
- 冲突对话框选「重新载入」被当作保存成功，导致关窗/新建时丢内容
- 未命名文档崩溃恢复后无法保存（绑错路径）、草稿永远清不掉
- 多窗口未命名文档草稿互相覆盖；手动保存与自动保存并发双写

### 修复 — 导出

- 导出 PDF/PNG/HTML 中本地图片全部丢失（相对路径在导出环境解析不到）
- docx 导出丢表格表头行；行内代码无代码样式；行内公式/图片/硬换行静默丢失

### 修复 — 安全

- 链接白名单可被控制字符绕过（`java&#10;script:` 形态的导出 HTML XSS）
- `file:write` 无扩展名限制（可覆写任意路径文件）→ 限定为可打开文档
- 图片落盘准入收紧（头部嗅探失败拒绝、大小预检、防文件名碰撞覆盖）
- 图片下载 SSRF 基础收紧（拒绝环回/私有地址）；窗口导航守卫；CSP 生产收紧

### 修复 — 体验 / 正确性

- 缩放后菜单弹出位置错位（缺 zoomFactor 换算）；历史残留的整页缩放让整体 UI 偏小（自愈归一）
- 源码模式真实回车丢换行（contenteditable 拆块 + 尾部光标锚定，双坑）
- 内容缩放下大纲跳转/源码定位/搜索滚动/打字机居中偏移（坐标单位混用）
- 搜索替换：posAtDOM 抛错中断、重叠匹配破坏正文、替换后高亮跳回第 1 处
- 拖入多个文件竞态；更新检查弹窗阻塞全部窗口；下载中关不掉进度卡
- 设置对话框 Esc 不还原焦点、数字输入中间态误写；Ctrl+H 连按状态错乱
- 背景图预览不加载、超大图设置不持久；图片粘贴失败无提示；源码模式插入图片丢失
- 图片引用含括号/# 的文件名编码断裂

### 工程

- 依赖升级：electron 44.0.0 → 44.4.5、katex → 0.18.9 等补丁级
- 安装包瘦身：asar 17007 → 2519 条目（-85%，renderer 依赖不再打进包）
- 测试 240 → 272 例（新增 32 条回归防线）；tests/ 纳入 typecheck；测试数据目录隔离
- 遗留事项清单见 TODO.md

## 历史版本（摘要）

- **0.7.0** — 偏好设置窗口、代码块语言角标/自动识别、代码块内符号补全
- **0.6.1** — 图片粘贴/插入显示修复、插入图片改原生文件选择
- **0.6.0** — 多窗口、数据安全三件套、会话恢复、公式/Mermaid/脚注/TOC、文件树、docx+PNG 导出、写作模式
- **0.5.1** — 背景图片壁纸新版、更新失败提示优化
- **0.3.x–0.5.0** — 早期版本（MarkLite 时期，0.5 起更名 Typewren）

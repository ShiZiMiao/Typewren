/* ============================================================
 * 顶级菜单标签（主进程菜单模板 与 渲染层自绘菜单栏 共用）
 * 两边标签必须一致，否则自绘栏点击弹不出对应子菜单。
 * ============================================================ */

export const MENU_TOP_LABELS = ['文件', '编辑', '格式', '段落', '视图', '帮助'] as const;

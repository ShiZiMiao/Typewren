# Typewren 后续版本规划（ROADMAP）

> 本文档记录优先级最高的待办（0.7.x 候选），以及低优先级的候选池。
> 每项给出「现状 / 目标 / 方案要点 / 验收标准」，供后续会话直接接续实施。
> 更新于 2026-09-18（0.6.1 发布后）。

## 第一优先（0.7.x 候选，按序做）

### 1. 偏好设置窗口 ✅ 已完成（见版本记录 0.7.x 草记）

### 2. 导出文件内嵌本地图片（自包含）

- **现状**：导出 HTML/PDF/PNG 走 schema toDOM 序列化，图片仍是 `./assets/…` 相对路径；
  导出 HTML 存到别处、PDF/PNG 走隐藏窗口临时目录时图片 404（编辑器内已修复，
  导出链路是最后一块）。
- **目标**：PDF / PNG 一律自包含（图片内联）；HTML 导出提供自包含选项
  （无选项前默认内联，保证"换位置不裂"）。
- **方案要点**：
  - 主进程新增 `image:read-data`（或复用协议读盘实现）：绝对路径 → { base64, mime }，
    渲染层在 exportDocument.ts 序列化后把 `<img src>` 替换为 `data:…` URI
    （仿 renderFragmentMath/highlightCodeBlocks 的后处理结构）。
  - 相对 `./assets/…` 以当前文档目录为基准解析绝对路径（复用 shared/imageUrl 的
    joinPath 逻辑）；未保存文档的绝对引用直接读。
  - 25MB 上限 + 只读图片扩展名（与 images.ts 落盘同一安全口径）。
  - docx 导出天然无此问题（结构序列化，图片另行处理时同理内联）。
- **验收标准**：含本地图片的文档导出 HTML / PDF / PNG，换目录打开/打印均显示图片；
  e2e 断言导出产物内含 data: 图片 URI。

### 3. 文件监视（fs.watch 实时外部修改提示）

- **现状**：fileService.save() 写盘前才比对磁盘与基线；其它程序改动文档时编辑器无感知，
  用户可能在旧内容上继续编辑。
- **目标**：打开（且有路径）的文档经 fs.watch 监听，外部修改/删除实时提示：
  弹出与现有「文件已被其它程序修改」相同三选（覆盖保存 / 重新载入磁盘 / 取消），
  或非模态提示条；文件被删除时明确提示。
- **方案要点**：
  - 主进程以 docRegistry（窗口↔路径表）为信号源，监听后广播
    `file:external-change`（带路径），渲染层 fileService 收广播后比对
    currentMarkdown vs 磁盘（readFileQuiet），不一致才提示。
  - 避免自激：Typewren 自身写盘（save/saveAs）期间暂时忽略 watch 事件；
    保存成功后重新登记监听（另存为换路径）。
  - 多窗口：同一文档被本应用两个窗口打开时只提示一次（docRegistry 去重）。
  - e2e：外部进程改写文件 → 应用内出现提示并可选重新载入。
- **验收标准**：外部改动 >1s 内出现提示；选择「重新载入」不丢当前未保存修改时先确认；
  Typewren 自己的保存不触发提示。

## 候选池（后续按需排期）

- 图片体验：点击放大预览（lightbox）、尺寸控制、右键「打开所在文件夹 / 替换图片」
- URL 自动成链（补 remark-gfm autolink literal，输入 www./http:// 实时转换）
- YAML frontmatter（标题/标签/摘要；remark-frontmatter，导出/编辑器展示可选字段）
- 文件树读写化（新建/重命名/删除/移动文档 + 引用与 assets 路径联动更新）
- 文件夹内全文搜索（当前只支持当前文档搜索栏）
- 表格增强：表头切换、单元格合并/拆分（prosemirror-tables 命令已具备）、列对齐 `:---:`
- 任务列表互转（普通列表 ↔ 待办）
- 单窗口多标签页（与现有"多文档=多窗口"形态需先定路线）
- 打印（系统打印对话框，复用导出管线）
- 粘贴 Word/HTML 清洗（HTML → Markdown 清洗层，避免富文本样式污染）
- 保存前历史备份（userData/history，兜底误覆盖）
- 自定义主题/CSS（用户级 theme 目录）
- 窗口位置/尺寸会话记忆（当前仅级联布局）
- 多篇文档拼接导出（选中若干文件 → 一个 PDF/HTML）

## 版本记录

- **0.6.1**：图片粘贴/插入显示修复
  - 编辑器内图片统一经 `typewren-img://` 本地协议按文档目录解析（相对/绝对路径均可用）
  - 未保存文档粘贴修复（Markdown 引用改正斜杠绝对路径，不再 `%5C` 污染）
  - 插入图片（Ctrl+Shift+I）改为原生文件选择框，与粘贴共用落盘链路
- **0.7.x 草记：偏好设置窗口**（第一优先 #1）
  - 「文件 → 偏好设置…」（Ctrl+,）→ 自研模态设置框，修改即时生效
  - 配置：编辑器字体族/字号/行距/宽度（CSS 变量 `--editor-font-*` / `--editor-max-width`）、
    默认主题（亮/暗/跟随系统）、自动保存(+间隔)、崩溃恢复草稿间隔、拼写检查、成对符号补全
  - 持久化 userData/settings.json（主进程 settings.ts），set 即广播全部窗口
  - 主题/拼写/成对符号保留 localStorage 镜像（首帧快路径 + 测试预置兼容），
    settings.json 权威；`typewren.legacy-migrated` 一次性迁移旧 localStorage 偏好
  - e2e：tests/settings.spec.ts（独立 --user-data-dir，9 用例覆盖读写/生效/自动保存/重启保留）
